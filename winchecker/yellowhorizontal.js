/* tjekker om der er nogen som vinder */
function checkClass(){
    if (
        circle1.classList.contains('yellow')
        && circle8.classList.contains('yellow')
        && circle15.classList.contains('yellow')
        && circle22.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle2.classList.contains('yellow')
        && circle9.classList.contains('yellow')
        && circle16.classList.contains('yellow')
        && circle23.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle3.classList.contains('yellow')
        && circle10.classList.contains('yellow')
        && circle17.classList.contains('yellow')
        && circle24.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle4.classList.contains('yellow')
        && circle11.classList.contains('yellow')
        && circle18.classList.contains('yellow')
        && circle25.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle5.classList.contains('yellow')
        && circle12.classList.contains('yellow')
        && circle19.classList.contains('yellow')
        && circle26.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle6.classList.contains('yellow')
        && circle13.classList.contains('yellow')
        && circle20.classList.contains('yellow')
        && circle27.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle7.classList.contains('yellow')
        && circle14.classList.contains('yellow')
        && circle21.classList.contains('yellow')
        && circle28.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    if (
        circle8.classList.contains('yellow')
        && circle15.classList.contains('yellow')
        && circle22.classList.contains('yellow')
        && circle29.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle9.classList.contains('yellow')
        && circle16.classList.contains('yellow')
        && circle23.classList.contains('yellow')
        && circle30.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle10.classList.contains('yellow')
        && circle17.classList.contains('yellow')
        && circle24.classList.contains('yellow')
        && circle31.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle11.classList.contains('yellow')
        && circle18.classList.contains('yellow')
        && circle25.classList.contains('yellow')
        && circle32.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle12.classList.contains('yellow')
        && circle19.classList.contains('yellow')
        && circle26.classList.contains('yellow')
        && circle33.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle13.classList.contains('yellow')
        && circle20.classList.contains('yellow')
        && circle27.classList.contains('yellow')
        && circle34.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }
    
    if (
        circle14.classList.contains('yellow')
        && circle21.classList.contains('yellow')
        && circle28.classList.contains('yellow')
        && circle35.classList.contains('yellow')
    ) {
       winnerPopup.classList.add('displaytrue');
dropdown.innerHTML = `tillykke ${playerTwo.innerHTML}`
    } else {
        
    }

    /* ali was here */
                    
    
}
setInterval(checkClass, 1000);
/* tjekker vinderen slut */